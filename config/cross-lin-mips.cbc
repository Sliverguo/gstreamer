import os
from cerbero.config import Platform, Architecture, Distro, DistroVersion

tools_prefix = '/opt/x1800/toolchains/mips-gcc520-32bit/bin/mips-linux-gnu-'
toolchain_prefix = '/opt/x1800/toolchains/mips-gcc520-32bit/'
target_platform = Platform.LINUX
target_distro = Distro.DEBIAN
target_arch = Architecture.MIPS
variants = ['noalsa', 'nox11', 'nopulse', 'nocdparanoia', 'nosdl',
            'nov4l2', 'notestspackage', 'nopython']
